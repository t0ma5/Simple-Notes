<?xml version="1.0" encoding="utf-8"?>
<tomato.simple.notes.views.SquareRelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/notebook_item_holder"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="@dimen/medium_margin"
    android:background="@drawable/note_card_background"
    android:paddingStart="@dimen/normal_margin"
    android:paddingTop="@dimen/normal_margin"
    android:paddingEnd="@dimen/normal_margin"
    android:paddingBottom="4dp">

    <ImageView
        android:id="@+id/notebook_lock_icon"
        android:layout_width="28dp"
        android:layout_height="28dp"
        android:layout_alignParentEnd="true"
        android:layout_alignParentTop="true"
        android:importantForAccessibility="no"
        android:src="@drawable/ic_lock_open_vector"
        android:visibility="gone" />

    <ImageView
        android:id="@+id/notebook_pinned_icon"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_alignParentEnd="true"
        android:layout_below="@+id/notebook_lock_icon"
        android:importantForAccessibility="no"
        android:src="@drawable/ic_pin_vector"
        android:visibility="gone" />

    <com.simplemobiletools.commons.views.MyTextView
        android:id="@+id/notebook_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:layout_marginEnd="32dp"
        android:ellipsize="end"
        android:lines="2"
        android:textSize="@dimen/big_text_size"
        android:textStyle="bold"
        tools:text="Notebook" />

    <ImageView
        android:id="@+id/notebook_icon"
        android:layout_width="51dp"
        android:layout_height="51dp"
        android:layout_centerInParent="true"
        android:importantForAccessibility="no"
        android:src="@drawable/ic_notebooks_vector" />

    <LinearLayout
        android:id="@+id/notebook_footer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <com.simplemobiletools.commons.views.MyTextView
            android:id="@+id/notebook_note_count"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:ellipsize="end"
            android:gravity="start|center_vertical"
            android:maxLines="1"
            android:textSize="@dimen/smaller_text_size"
            tools:text="4 notes" />

        <ImageView
            android:id="@+id/notebook_drag_handle"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:clickable="true"
            android:contentDescription="@string/notebook_actions"
            android:focusable="true"
            android:src="@drawable/ic_drag_indicator_vector" />
    </LinearLayout>

</tomato.simple.notes.views.SquareRelativeLayout>
